
use regex::Regex;

pub fn split_keeping_separator(regex: &Regex, content: &str) -> Vec<String> {
    /* regex.split(content).into_iter().map(|chapter_content| {
        chapter_content.to_string()
    }).collect() */

    todo!()
}